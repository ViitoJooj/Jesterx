apiVersion: v1
kind: ConfigMap
metadata:
  name: caddy-config
data:
  Caddyfile: |
    {$CADDY_DOMAIN::80} {
    	handle_path /api/* {
    		reverse_proxy daemon:8080
    	}

    	handle {
    		reverse_proxy view:80
    	}
    }
