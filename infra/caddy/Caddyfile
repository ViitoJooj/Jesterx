# Caddy reverse proxy for Verkoupe development

:80 {
    # API (daemon)
    handle /api/* {
        reverse_proxy daemon:8080
    }

    # Frontend (view - Vite dev server)
    handle {
        reverse_proxy view:5173
    }
}
